import 'package:flutter/material.dart';

import 'app_colors.dart';

class AppStyles {
  final TextStyle normal = TextStyle(
    fontSize: 15,
    color: AppColors().primary,
  );

  final TextStyle action = TextStyle(
    fontSize: 15,
    color: AppColors().primary,
    fontWeight: FontWeight.bold,
  );
}
